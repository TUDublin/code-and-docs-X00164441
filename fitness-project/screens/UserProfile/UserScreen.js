import {
  Pressable,
  StyleSheet,
  Text,
  View,
  SafeAreaView,
  ScrollView,
  Image,
} from "react-native";
import React from "react";

const UserScreen = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>UserScreen</Text>
      </ScrollView>
    </SafeAreaView>
  );
};

export default UserScreen;

const styles = StyleSheet.create({});
